# SystemOn — залежності для запуску та збірки .exe (Windows)
# Рекомендовано: Python 3.10+ (3.12 / 3.13)
#
#   pip install -r requirements.txt
#   build.bat

# --- Запуск програми ---
PyQt5>=5.15.0
psutil>=5.9.0

# --- Збірка portable .exe (Nuitka, див. build.bat) ---
nuitka>=2.4
ordered-set>=4.1.0
